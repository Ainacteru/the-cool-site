on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**'
      
jobs:
  deploy:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "Deploying Frontent!"
      - run: curl -d "d=front" ${{ secrets.LOCATION }}

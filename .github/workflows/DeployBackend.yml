on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      
jobs:
  deploy:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "Deploying Backend!"
      - run: curl -d "d=back" ${{ secrets.LOCATION }}
